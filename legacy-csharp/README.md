# legacy-csharp (보관용)

이 폴더는 QuickLaunch의 **초기 C# WinForms 프로토타입(ShortcutDeck)**입니다.
현재 메인 구현은 루트의 **Electron 버전**으로 대체되었으며, 이 코드는 참고용으로만 보관됩니다.

빌드하려면:

```bash
cd legacy-csharp
dotnet run
```

(`net7.0-windows`, WinForms 필요)
